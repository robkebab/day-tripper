<% if !@current_user.is_driver?(@trip) && !@current_user.is_passenger?(@trip) %>
<%= button_to "Join as a Driver", add_driver_path(@trip) %>

<% elsif @current_user.is_driver?(@trip) && !@current_user.is_organizer?(@trip) %>
<%= button_to "Leave Trip" %>
<% end %>